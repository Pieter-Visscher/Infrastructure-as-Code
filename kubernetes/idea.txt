create an API

create conjobs via the API for specific tasks
job includes:
	when via crontab
	playbook via github url
	specific ansible command?
	when job is decided launch it via curl as an kubernetes job. instert into container: inventory and playbooks
	possible to have it 

docker container
build with ansible componets
copy premade crontab?
integrate discord bot or email to send success or failure messages. overview of task status

